function main() {
    valid = "([</>])"
    invClose = "([</>]"
    invOrd = "(<[/>])"

    console.log(validate(valid))
    console.log(validate(invClose))
    console.log(validate(invOrd))
}

function validate(string) {
    const arr = [];
    const a = (     
    validate(valid)
    for (let char = 0; char < string.length-1; char++) {
        
        
    }
    return true
}


main()