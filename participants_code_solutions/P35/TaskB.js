function main(array) {
    parentheses = ["(", "<", "["]
    closinggparentheses = [")", "/>", "]"]
    anypresence = []
    result = {}
    array.each do |a| 
        if (a.includes? (parentheses) || (closingparentheses) {
            anypresence << a
        } else { 
            console.log('Not valid')
        }
    end 

    parentheses.each do |a| 
        if a.includes? (anypresence) 
            result << anypresence[index]
        end
    end

    
    closingparentheses.each do |a| 
        if a.includes? (anypresence) 
            result << anypresence[index]
        end
    end

    result.each do |r|
        




    
    console.log('Answer task B:')
}

main()