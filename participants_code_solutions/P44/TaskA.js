function main() {
    let roman = "XII"
    const array = Array.from(roman);

    array.forEach((letter) => {
        if (letter === 'X') {
           letter = 10}
        elsif (letter === 'I') {
            letter = 1
        }
        
}

const numerals = {'I': 1, 'V': 5, 'X': 10}

main()